#include <stdio.h>

void main(void){
    int ordem, lin, col;
    scanf("%d", &ordem);
    int mat[ordem][ordem];

        for (lin = 0; lin < ordem; lin++)
        {
            for (col = 0; col < ordem; col++)
            {

            }
        }
