#include <stdio.h>

int main()
{
    long long int fibo[60];
    int cont, i,num;
    fibo[0]=0;
    fibo[1]=1;

    scanf ("%d", &cont);
    for (i = 0; i < cont; i++)
    {
            scanf("%d", &num);
    }
}
